from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='We collaborate on doing small bits of practice as if we were working at a startup ',
    author='Learn-as-an-Intern',
    license='MIT',
)
